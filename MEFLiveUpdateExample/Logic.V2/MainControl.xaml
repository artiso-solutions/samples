<UserControl x:Class="Logic.MainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:logic="clr-namespace:Logic"
             Width="525"
             Height="350">
    <UserControl.Resources>
        <logic:ThrowExceptionCommand x:Key="ThrowExceptionCommand"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Content="V2" FontSize="72" Grid.Row="0" />
        <Button Grid.Row="1" Content="Throw Exception" Margin="20" Command="{StaticResource ThrowExceptionCommand}"/>
    </Grid>

</UserControl>
